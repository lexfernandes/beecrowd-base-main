export function problem (lines) {
    
    const a = lines[0]
    const b = lines[1]
    const c = lines[2]
    const d = lines[3]

   
    let difference = (Number(a * b) - Number(c * d))

    console.log(`DIFERENCA = ${difference}`)
   
 }